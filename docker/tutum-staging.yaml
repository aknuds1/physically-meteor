mongo-staging:
  image: mongo:3
  command: --auth
  volumes:
    - /var/data/mongodb:/data/db
  ports:
    - "27017:27017"
  tags:
    - physically-staging
  autorestart: always
physically-staging:
  image: tutum.co/aknuds1/physically:latest
  links:
    - mongo-staging:db
  environment:
    - ROOT_URL=https://physically-app.com
    - MONGO_URL
    - METEOR_SETTINGS
  tags:
    - physically-staging
  autorestart: always
  ports:
    - "80:80"
logspout-staging:
  image: gliderlabs/logspout
  command: syslog://logs3.papertrailapp.com:19740
  tags:
    - physically-staging
  autorestart: always
  volumes:
    - /var/run/docker.sock:/tmp/docker.sock
  deployment_strategy: every_node
